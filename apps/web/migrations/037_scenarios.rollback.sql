-- E06-S04 rollback: flat scenarios and overrides (synthetic only).
-- Drop code first, then schema. Forbidden after real scenarios exist.

DROP TABLE IF EXISTS scenario_overrides;
DROP TABLE IF EXISTS scenarios;
